{
  "private": true,
  "workspaces": [
    "./apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "~1.1.3"
  },
  "packageManager": "yarn@3.2.0"
}
